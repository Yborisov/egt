export const countryToVatPercentMap: { [key: string]: number } = {
  Bulgaria: 0.2,
  Romania: 0.19,
};

export const cartlessRoutes = ['/cart', '/checkout'];

export const requestStatusSuccess = 'fulfilled';
